services:
  web:
    environment:
      GO_API_URL: ${GO_API_URL:-http://go:8080}
    ports: []

  go:
    environment:
      PYTHON_API_URL: http://python:8000
      JWT_SECRET: ${JWT_SECRET}
      ANALYTICS_API_SECRET: ${ANALYTICS_API_SECRET}
      OWNWAVE_COOKIE_SECURE: "true"
      ALLOWED_ORIGINS: https://${OWNWAVE_DOMAIN:-localhost}
      PUBLIC_APP_URL: https://${OWNWAVE_DOMAIN:-localhost}
      OWNWAVE_DOMAIN: ${OWNWAVE_DOMAIN:-localhost}
    ports: []

  python:
    ports: []

  db:
    ports: []

  redis:
    ports: []

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    environment:
      OWNWAVE_DOMAIN: ${OWNWAVE_DOMAIN:-localhost}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web
      - go

volumes:
  caddy_data:
  caddy_config:
